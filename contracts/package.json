{
  "name": "canhav-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "CanHav Research — ERC-8004 agent registries (OpenZeppelin) on Arbitrum Sepolia",
  "scripts": {
    "build": "forge build",
    "test": "forge test -vvv",
    "fmt": "forge fmt",
    "check:deployer": "bash scripts/check-deployer.sh",
    "deploy:arbitrum-sepolia": "bash scripts/check-deployer.sh && forge script script/Deploy.s.sol:Deploy --rpc-url arbitrum_sepolia --broadcast --slow",
    "deploy:arbitrum-sepolia:verify": "bash scripts/check-deployer.sh && forge script script/Deploy.s.sol:Deploy --rpc-url arbitrum_sepolia --broadcast --slow --verify"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0"
  }
}
