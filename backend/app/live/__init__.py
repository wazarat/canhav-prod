"""
Live data overlays — DEFERRED to Step 4.

These modules will enrich staged profiles with on-chain circulating supply
(Alchemy) and macro peg history (Dune), using free-tier APIs only. They are
stubs for now: signatures + docs are in place so the next agent can implement
them without re-deriving the contract. Nothing here is called during Steps 1-3.
"""
