// Stub for the `server-only` package under Jest (Node env). The real module
// throws when imported outside a React Server Component; our server clients
// import it for lint-time enforcement only, so an empty module is safe in tests.
export {};
