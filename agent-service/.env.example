# Copy to `agent-service/.env` and fill in. `.env` is gitignored.
# NETWORK: Arbitrum Sepolia ONLY (chainId 421614). Testnet only.

# Public RPC for read calls (gate checks, event parsing).
ARBITRUM_SEPOLIA_RPC_URL=https://sepolia-rollup.arbitrum.io/rpc

# Deployed registry addresses (from `contracts` Deploy.s.sol output).
IDENTITY_REGISTRY_ADDRESS=
SECURITY_REGISTRY_ADDRESS=

# Optional: Arbiscan API key for source-verification gating (off-chain check).
ARBISCAN_API_KEY=

# --- Frontend agent layer (Next.js) ----------------------------------------
# The web app deep-imports this package for agent types + the research runners
# (frontend/lib/agent/verdictRunner.ts). When deployed on Vercel, the SAME
# on-chain vars above (IDENTITY_REGISTRY_ADDRESS, SECURITY_REGISTRY_ADDRESS)
# must be set as frontend project env vars — server routes read process.env.
# The research loop needs OPENAI_API_KEY (documented in backend/.env.example).
