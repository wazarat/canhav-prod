# CanHav Research — backend dependencies
#
# NOTE: the ingestion scripts and the LocalAdapter run on the Python standard
# library ALONE — you can run them with no installs (DB_BACKEND=local). The
# packages below are for the Upstash Redis backend and typed models.

# Typed models
pydantic>=2.6,<3

# Redis backend (DB_BACKEND=redis) — point REDIS_URL at Upstash (rediss://...)
redis>=5

# Live overlays (Alchemy / CoinGecko) use the standard library (urllib) — no
# extra HTTP dependency required.
